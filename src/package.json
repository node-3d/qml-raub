{
	"name": "build",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "node-gyp rebuild && node -e \"require('addon-tools-raub/cpbin')('qml')\""
	},
	"dependencies": {
		"addon-tools-raub": "5.0.0",
		"deps-qmlui-raub": "2.0.0"
	}
}
